let person = new Array("Kristian", "Lars", "Lars", "Marius", "Krsitian");
let age = new Array(17, 18, 18, 18, 17);

for (let i = 0; i < person.length; i++) {
    console.log(`${person[i]} is ${age[i]} years old.`);
}